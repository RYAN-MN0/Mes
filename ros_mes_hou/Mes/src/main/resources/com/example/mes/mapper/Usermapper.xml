<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.mapper.Usermapper">
    <select id="selectAccount" resultType="com.example.mes.pojo.User">
        select account,password,token,updateToken from userinfo where account = #{account} and password = #{password}
    </select>
    <insert id="insertAccount">
        insert into userinfo(account, password, token, updateToken) values(#{account},#{password},#{token},#{updateToken})
    </insert>
    <update id="updateToken">
        update userinfo set token = #{token}, updateToken = #{updateToken} where account = #{account}
    </update>
</mapper>